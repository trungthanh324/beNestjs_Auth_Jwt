import exp from "constants";

export enum HttpCode{
    SUCCESS = 200,
    ERROR = 404
}

export enum HttpMessage{
    SUCCESS = "SUCCESS",
    ERROR = "ERROR"
}

export enum Roles{
    ADMIN = "ADMIN",
    MOD = "MOD",
    USER = "USER"
}